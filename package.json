{
  "name": "sheta-dev-blog",
  "private": true,
  "type": "module",
  "scripts": {
    "diagrams": "node scripts/generate-diagrams.mjs",
    "social-cards": "node scripts/generate-social-card.mjs",
    "assets": "npm run diagrams && npm run social-cards",
    "predev": "npm run assets",
    "dev": "astro dev",
    "prebuild": "npm run assets",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "check:links": "node scripts/check-external-links.mjs",
    "validate:live": "node scripts/validate-live.mjs",
    "validate": "npm run check && npm run build && node scripts/validate-build.mjs && node scripts/validate-worker.mjs"
  },
  "dependencies": {
    "astro": "^7.2.2",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.123.0"
  }
}
